class TtsSttService {
  // Pure Dart abstraction for native platform bridge handling
  Future<void> speak(String text) async {
    // Handled via native platform channel methods in pure Flutter code
  }

  Future<String> listen() async {
    // Handled via native platform channel speech recognizer bindings
    return '';
  }
}
